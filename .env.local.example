# Convex Configuration
# Get these values from https://dashboard.convex.dev after creating a project
VITE_CONVEX_URL=https://your-project.convex.cloud

# Optional: OpenAI API Key (stored client-side, encrypted in IndexedDB)
# VITE_OPENAI_API_KEY=

# Optional: Anthropic API Key (stored client-side, encrypted in IndexedDB)
# VITE_ANTHROPIC_API_KEY=
